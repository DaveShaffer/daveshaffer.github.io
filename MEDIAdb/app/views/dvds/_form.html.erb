<%= form_for(@dvd) do |f| %>
  <% if @dvd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dvd.errors.count, "error") %> prohibited this dvd from being saved:</h2>

      <ul>
        <% @dvd.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end # End messages.each do %>
      </ul>

    </div>
  <% end # End if @dvd.errors %>

  <div class="field">
    <%= f.label :format %><br>
    <%= f.text_field :format %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end # End form_for do %>
